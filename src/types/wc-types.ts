export interface Result {
  id?: string;
  name: string;
  description: string;
}

export interface Contact{
  contactName: string;
  contactEmail: string;
  contactMobile: string;
  ticketMessage: string;
}